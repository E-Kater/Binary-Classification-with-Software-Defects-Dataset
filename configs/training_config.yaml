training:
  max_epochs: 50
  patience: 10
  monitor: "val_f1"
  mode: "max"
  accelerator: "cpu"
  devices: 1
  optimizer:
    weight_decay: 0.001

mlflow:
  tracking_uri: "mlruns"
  experiment_name: "software_defects"
